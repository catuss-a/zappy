/*
** action_gauche.c for gauche in /home/catuss_a//workspace/zappy/server_src
** 
** Made by axel catusse
** Login   <catuss_a@epitech.net>
** 
** Started on  Sun Jun 12 19:41:10 2011 axel catusse
** Last update Sun Jul 10 16:23:27 2011 bruno quilghini
*/

#include <stdio.h>
#include <string.h>

#include "game.h"

static void	to_graphic(t_game *game, t_player *player)
{
  char		buff[512];

  (void)game;
  bzero(buff, sizeof(buff));
  sprintf(buff, G_PLAYER_POS,
	  player->num,
	  player->pos.x,
	  player->pos.y,
	  player->direction);
  send_to_graphic(game, buff);
}

int		action_gauche(t_game *game, t_player *player,
			      __attribute__((unused))unsigned int idx_action)
{
  printf("gauche\n");
  if (player->direction == NORD)
    player->direction = OUEST;
  else
    player->direction -= 1;
  to_graphic(game, player);
  sock_send(player->sock, OK);
  return (SUCCESS_VALUE);
}
